[build]
builder = "nixpacks"

[deploy]
startCommand = "APP_MODE=web node src/server.js"
# /health отвечает сразу, без ожидания статических файлов
healthcheckPath = "/health"
healthcheckTimeout = 60
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 5
